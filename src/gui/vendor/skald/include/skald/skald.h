#pragma once

// skald — single-include umbrella header. Drag this in once and the
// whole library (tokens + theme + widgets) is reachable through the
// `skald::` namespace.

#include <skald/fonts.h>
#include <skald/icons.gen.h>
#include <skald/theme.h>
#include <skald/tokens.h>
#include <skald/widgets.h>
