# X-line sections

An x-line section is a contiguous source-index range selected for fitting.
Sections can be adjusted by index or by nanometer position.

Auto-Fit All attempts to tile the selected range with fitted wave sections.
Each fitted section stores its own sample spacing and wave parameters.
