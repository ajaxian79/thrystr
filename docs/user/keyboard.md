# Keyboard

Space toggles playback. Left and right arrows move the playhead by one sample.

Ctrl or Cmd plus N opens a new workspace. Ctrl or Cmd plus A creates a wave.
Ctrl or Cmd plus W also creates a wave.
