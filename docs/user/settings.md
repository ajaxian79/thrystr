# Settings

Settings control optional overlays and render toggles. The Inspector overlay
can be enabled when you need live analysis and fit counters.

Zoom controls affect the view only. Data point spacing and wave wavelength are
entity parameters and remain independent.
