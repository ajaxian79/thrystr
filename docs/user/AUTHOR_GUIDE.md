# Author guide

Manual pages use a small Markdown subset: headings, paragraphs, bullet lists,
ordered lists, inline code, fenced code blocks, and links.

The doc generator sorts pages by filename for deterministic output. Keep page
titles unique so search results and generated anchors remain clear.
