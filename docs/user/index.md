# thrystr manual

thrystr is a scalar wave workspace for loading byte data, mapping it into
sample values, and comparing that data against editable wave entities and
fitted section encodings.

Start with a source file, create an x-line section, then add waves or run
auto-fit to build a compact reconstruction over the selected samples.
