# Fit validation

Fit validation checks residuals between fitted waves and loaded data samples.
For track workspaces, validation also checks ownership and parity routing.

Residuals above tolerance are reported with the sample index so the failing
range can be inspected in the plot.
