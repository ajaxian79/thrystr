# Interpolated waves

An interpolated wave starts from selected data and searches for a base wave
that intersects the most selected points.

When a single wavelength cannot cover the selection, thrystr can add sparse
wavelength modifiers along the x axis.
