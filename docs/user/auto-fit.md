# Auto-fit

Auto-fit builds fitted sections over the current selection. Encoding can take
longer than decoding because thrystr searches for compact deterministic
section parameters.

The current single-track fitter runs in the background and can be cancelled.
When it completes, validation reports the section count and residuals.
